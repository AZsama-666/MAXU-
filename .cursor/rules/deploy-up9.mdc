---
description: 项目更新后需同步推送到 www.up9.life
alwaysApply: true
---

# 部署与同步规则

- **更新后同步到 up9.life**：对本项目（MAXU 展示页）做任何代码或文档更新并提交后，应推送到 Git 的 `main` 分支，以便自动部署到 **www.up9.life**。
- **部署方式**：仓库已配置 GitHub Actions（`.github/workflows/deploy-pages.yml`），`main` 分支有 push 时会构建并发布到 gh-pages，自定义域为 `www.up9.life`。无需额外步骤，推送即触发部署。
- **建议流程**：完成修改 → `git add` / `git commit` → `git push origin main`；如需打节点可再 `git tag` 并 `git push origin <tag>`。推送后约 1–2 分钟可在 https://www.up9.life 看到更新。
